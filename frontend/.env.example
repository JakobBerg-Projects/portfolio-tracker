# Copy this file to `.env.local` and adjust if needed. `.env.local` is gitignored.

# Base URL of the FastAPI backend.
# Note: local dev also proxies /api/* to the backend via next.config.ts.
NEXT_PUBLIC_API_URL=http://localhost:8000
